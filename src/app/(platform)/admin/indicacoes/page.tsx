import { ReferralStatus } from "@/components/platform/AdminForms";
import { PageHeader } from "@/components/platform/AppShell";
import { listReferrals } from "@/lib/platform/repository";
export default async function ReferralsPage(){const referrals=await listReferrals();return <><PageHeader eyebrow="Pipeline consentido" title="Indicações" description="Acompanhe somente candidatos que o dono do relacionamento escolheu compartilhar."/><div className="space-y-4">{referrals.length===0?<div className="rounded-2xl border border-dashed border-[var(--line)] bg-[var(--surface)] p-7 text-center sm:p-12"><p className="text-lg">Nenhuma indicação recebida</p><p className="mx-auto mt-2 max-w-md text-sm leading-6 text-[var(--muted)]">Quando um membro confirmar uma oportunidade, o candidato e as evidências autorizadas aparecerão aqui.</p></div>:referrals.map(r=><article key={r.id} className="rounded-2xl border border-[var(--line)] bg-[var(--surface)] p-4 sm:p-5"><div className="flex flex-col justify-between gap-4 md:flex-row md:items-center"><div className="min-w-0"><p className="font-medium break-words">{r.candidateName}</p><p className="mt-1 text-sm text-[var(--muted)] break-words">{r.candidateHeadline||"Perfil profissional compartilhado"}</p><p className="mt-3 text-xs text-[#aeb4c0] break-words">Indicada por <span className="text-white">{r.memberName}</span> para <span className="text-[#9eb2ff]">{r.jobTitle}</span></p>{r.relationshipNote&&<p className="mt-3 rounded-lg border border-[var(--line)] bg-[#0b0d12] p-3 text-xs text-[var(--muted)] break-words">“{r.relationshipNote}”</p>}</div><ReferralStatus id={r.id} current={r.status}/></div></article>)}</div></>}
