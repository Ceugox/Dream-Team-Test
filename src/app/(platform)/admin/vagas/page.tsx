import { CreateJobForm } from "@/components/platform/AdminForms";
import { PageHeader } from "@/components/platform/AppShell";
import { listJobs } from "@/lib/platform/repository";
import Link from "next/link";
export default async function JobsPage({searchParams}:{searchParams:Promise<{nova?:string}>}){const [jobs,params]=await Promise.all([listJobs(),searchParams]);const showForm=params.nova==="1"||jobs.length===0;return <><PageHeader eyebrow="Portfólio de talentos" title="Vagas" description="Publique as oportunidades que sua equipe pode destravar através de relações reais." action={!showForm?<Link href="/admin/vagas?nova=1" className="accent-button rounded-xl px-5 py-3 text-sm font-medium">+ Nova vaga</Link>:undefined}/>{showForm&&<section className="mb-7 rounded-2xl border border-[var(--line)] bg-[var(--surface)] p-5 md:p-6"><h2 className="mb-5 text-lg font-medium">Nova vaga</h2><CreateJobForm/></section>}<section className="grid gap-4 lg:grid-cols-2">{jobs.map(job=><article key={job.id} className="rounded-2xl border border-[var(--line)] bg-[var(--surface)] p-5"><div className="flex items-start justify-between gap-4"><div><span className="rounded-full bg-[#183128] px-2.5 py-1 text-[10px] text-[var(--green)]">Ativa</span><h2 className="mt-4 text-lg font-medium">{job.title}</h2><p className="mt-1 text-sm text-[var(--muted)]">{job.company}{job.location?` · ${job.location}`:""}</p></div><div className="text-right"><p className="text-2xl">{job.referralCount}</p><p className="text-[10px] text-[var(--muted)]">indicações</p></div></div><p className="mt-5 line-clamp-3 text-xs leading-5 text-[#aeb4c0]">{job.description}</p></article>)}</section></>}
